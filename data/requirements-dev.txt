# Tests (python -m pytest data/tests) : memes versions que l'image, y compris
# les dependances indirectes (constraints.txt), sans mediapipe (simule par
# data/tests/conftest.py).
-c constraints.txt
pytest>=8,<9
packaging
numpy
scipy==1.18.1   # reference des tests de reechantillonnage (absente de l'image)
flask
flask-socketio
requests
paho-mqtt
